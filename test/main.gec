import "module.gec"

#main:
  usemod $GecMath $gmath
  usemod $GecString $gstr

  buf "Please enter a number to continue: "
  call #gstr.print:

  input
  call #gmath.factorial $:
  dump $

  halt 0
end